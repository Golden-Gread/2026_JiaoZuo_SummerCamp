#include <bits/stdc++.h>
using namespace std;

int main(){
    int n;
    while(scanf("%d\n",&n)!=EOF){
        vector<vector<int>> tree(n+1,vector<int>());
        vector<vector<int>> dp(n+1,vector<int>(n+1,0));
        int u,childNum;
        for(int i=0;i<n;i++){
            
            scanf()

        }
    }
}